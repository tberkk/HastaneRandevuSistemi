@model List<Randevu>
@using Microsoft.AspNetCore.Mvc.Localization
@inject IViewLocalizer Localizer
@{
    ViewData["Title"] = "Randevular";
    Layout = "~/Views/Shared/_LayoutUser.cshtml";
}

<h4>@ViewBag.data</h4>
<table class="table table-striped">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">@Localizer["Gün"]</th>
            <th scope="col">@Localizer["Saat"]</th>
            <th scope="col">@Localizer["Doktor"]</th>
        </tr>
    </thead>
    <tbody>
        @{
            var i = 1;
        }
        @foreach (var x in Model)
        {
            <tr>
                <th>@i</th>
                @{
                    i++;
                }
                <td>@x.RandevuGun</td>
                <td>@x.RandevuSaat</td>
                <td>@x.Doktor.DoktorAd @x.Doktor.DoktorSoyad</td>
                <td><a href="/Home/RandevuAl/@x.RandevuID" class="btn btn-outline-primary ">+</a></td>
            </tr>
        }
    </tbody>
</table>